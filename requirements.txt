SQLAlchemy>=2.0
Flask>=3.0
pytest>=8

# --- Модуль приключения (опционально; см. pyproject [adventure]) ---
# httpx — клиент к локальному OpenAI-совместимому LLM-серверу (Ollama/vLLM/...)
# numpy — косинусная близость для семантического поиска
# sentence-transformers тянет torch/transformers (тяжёлые) и скачивает модель
# эмбеддера с HuggingFace при первом запуске. Ставьте отдельно, если нужен RAG:
#   pip install "httpx>=0.27" "numpy>=1.24" "sentence-transformers>=2.2"
